{% extends "base.html.twig" %}

{% block body %}

    {{ form_start(form) }}

    <div class="form-group">
        {{ form_label(form.sondage) }}
        {{ form_widget(form.sondage) }}
    </div>

    <div class="form-group">
        {{ form_label(form.user) }}
        {{ form_widget(form.user) }}
    </div>

    <div class="form-group">
        {{ form_label(form.selectedOption) }}
        {{ form_widget(form.selectedOption) }}
    </div>

    <div class="form-group">
        <button type="submit" class="btn btn-primary">Enregistrer Vote</button>
    </div>

    {{ form_end(form) }}

{% endblock %}
